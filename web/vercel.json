{
  "installCommand": "curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh && bun install",
  "buildCommand": "cd .. && wasm-pack build crates/gt-wasm --target web --out-dir ../../web/src/lib/wasm/pkg && cd web && bun run build",
  "headers": [
    {
      "source": "/(.*)\\.wasm",
      "headers": [
        { "key": "Content-Type", "value": "application/wasm" }
      ]
    }
  ]
}
