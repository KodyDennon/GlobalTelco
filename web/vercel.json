{
  "installCommand": "echo \"GlobalTelco web is retired; skipping dependency install\"",
  "buildCommand": "mkdir -p build && cp static/retired.html build/index.html && cp static/robots.txt build/robots.txt",
  "outputDirectory": "build",
  "cleanUrls": true,
  "redirects": [
    {
      "source": "/(.*)",
      "destination": "https://github.com/KodyDennon/GlobalTelco",
      "permanent": false
    }
  ]
}
